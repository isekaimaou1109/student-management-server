{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "bun",
			"request": "launch",
			"name": "Debugger for Bun",
			"program": "${workspaceFolder}/src/main.js",
			"cwd": "${workspaceFolder}",
			"watchMode": true,
			"runtime": "bun"
		}
	]
}